{
  "name": "frontend",
  "version": "0.1.0",
  "proxy": "http://localhost:5000",
  "private": true,
  "scripts": {
    "dev": "PORT=7200 next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
